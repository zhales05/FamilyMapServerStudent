package Services;

/**
 * clears database and then loads data onto database
 */
public class Load {
    /**
     * wipe database and reload with new info
     * @return whether operation was successful
     */
    public boolean clearAndLoadData() {
        return false;
    }
}
